Employees

	•	emp_no INT PK
	•	emp_title_id VARCHAR(10)
	•	birth_date DATE 
	•	first_name VARCHAR(35) 
	•	last_name VARCHAR(35) 
	•	sex VARCHAR(1) 
	•	hire_date DATE 

Departments

	•	dept_no VARCHAR(8) PK
	•	dept_name VARCHAR(40)

Salaries

	•	emp_no INT PK FK → Employees.emp_no
	•	salary INT

Titles

	•	title_id VARCHAR(10) PK
	•	title VARCHAR(40) 


Dept_Emp

	•	emp_no INT FK → Employees.emp_no
	•	dept_no VARCHAR(8) FK → Departments.dept_no

Dept_Manager

	•	emp_no INT FK → Employees.emp_no
	•	dept_no VARCHAR(8) FK → Departments.dept_no

Relationships Overview:

	1.	Employees have:
	•	One-to-Many relationship with Salaries, Titles, and Dept_Emp.
	•	One-to-One relationship with Dept_Manager (through the combination of dept_no and emp_no in the primary key).
	2.	Departments:
	•	One-to-Many relationship with Dept_Emp and Dept_Manager.
	3.	Salaries and Titles:
	•	Both reference the Employees table via a foreign key on emp_no.
	4.	Dept_Emp and Dept_Manager:
	•	Each table links employees to departments via emp_no and dept_no, enforcing many-to-many relationships between Employees and Departments.

